<%= link_to gravatar_for(current_user, size: Settings.size_gravatar_entry), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_my_profile"), current_user %></span>
<span><%= pluralize(current_user.entries.count, t(".entry")) %></span>
